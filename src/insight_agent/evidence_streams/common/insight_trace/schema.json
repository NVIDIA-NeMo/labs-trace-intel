{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nvidia.com/insight-agent/schemas/insight-trace/v1",
  "title": "Insight Agent canonical trace (insight-trace/v1)",
  "description": "One complete agent trace. A corpus is a JSONL file with one of these objects per line. This is the single format every dataset adapter targets; the shared loader fans one record out into the IA2 and IA3 engine contracts. Only four top-level fields and four per-call fields are required. Every optional field degrades gracefully, and each description below states exactly what omitting it costs.",
  "type": "object",
  "required": [
    "schema_version",
    "trace_id",
    "calls"
  ],
  "additionalProperties": false,
  "properties": {
    "schema_version": {
      "const": "insight-trace/v1",
      "description": "Format gate. Must be the literal string 'insight-trace/v1'."
    },
    "trace_id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable identifier for this trace, unique across the corpus. Used as the join key in every IA2 and IA3 output."
    },
    "calls": {
      "type": "array",
      "description": "Ordered tool calls. May be empty, in which case IA2 features are all zero and IA3 emits nothing for this trace.",
      "items": {
        "$ref": "#/$defs/call"
      }
    },
    "steps": {
      "type": "array",
      "description": "Observable trajectory steps, including non-tool steps such as planning, agent turns and evaluation boundaries. OMITTING THIS CHANGES IA2 NUMBERS: trajectory tokens fall back to one 'tool:{name}' token per call and trajectory_step_count becomes the call count, so clusters and anomaly scores shift. Two adapters over the same corpus, one emitting steps and one not, are not comparable.",
      "items": {
        "$ref": "#/$defs/step"
      }
    },
    "source_pointer": {
      "type": "object",
      "description": "Free-form pointer back to the original record (dataset, run id, URI, offsets). Carried verbatim into every digest row and finding. Omitting it makes the digest print 'not supplied' and leaves the Analyst unable to reopen the evidence.",
      "additionalProperties": true
    },
    "logical_case_id": {
      "type": [
        "string",
        "null"
      ],
      "description": "Identifier of the underlying task this trace attempts. Several traces of the same task share one logical_case_id. Defaults to trace_id when omitted, WHICH INFLATES independent_case_count: IA3 promotes a card only after three independent logical cases, so omitting this turns N retries of one task into N 'independent' cases and makes the precision gate meaningless."
    },
    "observed_verdict": {
      "type": [
        "string",
        "null"
      ],
      "description": "Explicit terminal verdict recorded by the harness, e.g. 'completed' or 'dead_end: no artifact produced'. Grouped as evidence by IA2 stage 5. Omitting it makes the digest's terminal-verdict section print an explicit abstention. Never used for anomaly selection or trajectory clustering."
    },
    "cost": {
      "type": [
        "number",
        "null"
      ],
      "description": "Total known cost of the trace in whatever unit the venue uses. Feeds verdict-group cost totals and representative ranking only."
    },
    "metrics": {
      "type": "object",
      "description": "Extra numeric features merged into the IA2 anomaly-detection feature vector. Keys MUST NOT collide with the eleven built-in feature names, because a collision silently replaces a measured feature. Values must be finite numbers.",
      "additionalProperties": {
        "type": "number"
      }
    },
    "tool_catalog": {
      "type": [
        "object",
        "null"
      ],
      "description": "Maps tool name to the JSON Schema for that tool's arguments. HIGHEST-VALUE OPTIONAL FIELD: omitting it makes six IA3 rules abstain (unknown_tool, missing_required_argument, unknown_argument, argument_type_mismatch, argument_enum_violation, json_schema_violation). malformed_tool_call still fires for non-object arguments with or without a catalog. A null value for a specific tool name means 'this tool is known but its schema is unavailable', which enables unknown_tool detection while abstaining from argument checks.",
      "additionalProperties": {
        "oneOf": [
          {
            "type": "object",
            "additionalProperties": true
          },
          {
            "type": "null"
          }
        ]
      }
    },
    "complete_provenance_context": {
      "type": "boolean",
      "default": false,
      "description": "Assert that this record captures EVERY user message and EVERY prior tool result for the trace. Only then can IA3 conclude an identifier was ungrounded rather than merely unseen. Setting this true without genuinely complete capture produces false positives; leaving it false makes explicitly_rejected_ungrounded_identifier abstain."
    },
    "task_text": {
      "type": "string",
      "default": "",
      "description": "The originating user request. Used as the fallback for each call's prior_user_text when that is not set per call."
    },
    "orphan_results": {
      "type": "array",
      "description": "Tool results observed with no matching call. Evidence of instrumentation loss. Without this, orphan_tool_result never fires.",
      "items": {
        "$ref": "#/$defs/orphan_result"
      }
    },
    "extra": {
      "type": "object",
      "description": "Free-form vendor passthrough. Ignored by every engine. Exists so the rest of the record can stay strict: any unrecognised key outside 'extra' is an error, which turns a typo such as 'tool_catelog' into a loud failure instead of six silently disabled rules.",
      "additionalProperties": true
    }
  },
  "$defs": {
    "call": {
      "type": "object",
      "required": [
        "call_id",
        "call_index",
        "tool_name",
        "arguments"
      ],
      "additionalProperties": false,
      "properties": {
        "call_id": {
          "type": "string",
          "minLength": 1,
          "description": "Identifier for this call, unique WITHIN the trace. If the source has no call ids, synthesise them as '{trace_id}#{call_index}'. A constant or repeated value fires duplicate_call_id on every call."
        },
        "call_index": {
          "type": "integer",
          "minimum": 0,
          "description": "Zero-based position of this call in the trace. Used as the ordering key by both engines."
        },
        "tool_name": {
          "type": "string",
          "minLength": 1,
          "description": "Name of the invoked tool, matching the keys of tool_catalog. Drives catalog lookup, dominant_tool_share and code_execution_share."
        },
        "arguments": {
          "description": "The arguments as the agent supplied them. EMIT RAW: a non-object value is legitimate evidence and produces malformed_tool_call. Do not parse a JSON-string argument blob into an object unless the source runtime itself parsed it, and do not stringify an object."
        },
        "result": {
          "description": "The tool's result. KEY ABSENCE IS SEMANTIC: omitting 'result' entirely means no result was ever recorded and fires missing_tool_result, whereas \"result\": null means the tool genuinely returned null. If the result is text, emit a plain string or {\"content\": \"...\"} — use 'content', never 'output' or 'message', because IA3 only unwraps 'content' and will otherwise see serialised JSON and miss error prefixes and tracebacks."
        },
        "result_missing": {
          "type": "boolean",
          "description": "Explicit escape hatch meaning 'no result was recorded', for emitters that cannot omit a key. Equivalent to omitting 'result'."
        },
        "result_count": {
          "type": "integer",
          "minimum": 0,
          "default": 1,
          "description": "How many results were observed for this call. 0 is treated as missing; values above 1 fire duplicate_tool_result."
        },
        "result_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "The identifier the result itself claims to answer. ONLY populate this when the source has a genuinely independent result-to-call reference. Filling it from a different id namespace (a message id, say) fires call_result_id_mismatch on essentially every call."
        },
        "duration_ms": {
          "type": [
            "number",
            "null"
          ],
          "minimum": 0,
          "description": "Wall-clock duration of the call in milliseconds. Feeds tool_duration_sec_total."
        },
        "explicit_error": {
          "type": [
            "boolean",
            "null"
          ],
          "default": null,
          "description": "Authoritative success flag from the source, when one exists. true forces a failure; FALSE FORCES SUCCESS AND DISABLES ALL TEXT-BASED FAILURE DECODING for this call. Prefer null (or omit) unless the source really carries a reliable flag, otherwise explicit_tool_failure goes silent across the whole corpus."
        },
        "outcome_marker": {
          "type": [
            "string",
            "null"
          ],
          "description": "Names the failure mechanism when explicit_error is true. Becomes the card's mechanism_key, so it determines card identity."
        },
        "instrumentation_alias_of": {
          "type": [
            "string",
            "null"
          ],
          "description": "The real tool name when tool_name is an instrumentation artefact. Fires mapped_instrumentation_alias."
        },
        "prior_user_text": {
          "type": "string",
          "default": "",
          "description": "User text visible before this call, used to decide whether an identifier was grounded in the request. Falls back to the trace-level task_text."
        },
        "returned_data": {
          "type": "boolean",
          "description": "False means the call succeeded but returned no rows or matches. Drives returned_data_false_rate. The loader injects this into the result mapping, so it has no effect when the result is a bare string."
        },
        "source_pointer": {
          "type": "object",
          "description": "Free-form pointer back to this specific call in the source record. Carried verbatim into every finding.",
          "additionalProperties": true
        },
        "extra": {
          "type": "object",
          "description": "Free-form vendor passthrough for this call. Ignored by every engine.",
          "additionalProperties": true
        }
      }
    },
    "step": {
      "type": "object",
      "required": [
        "step_index",
        "step_type"
      ],
      "additionalProperties": false,
      "properties": {
        "step_index": {
          "type": "integer",
          "minimum": 0,
          "description": "Zero-based position of this step in the trajectory."
        },
        "step_type": {
          "type": "string",
          "minLength": 1,
          "description": "Kind of step. Conventional values are 'tool', 'agent', 'planning', 'evaluation' and 'user'. Steps typed 'evaluation' collapse to a single token during clustering so per-run verdict text does not fragment the clusters."
        },
        "name": {
          "type": "string",
          "default": "",
          "description": "Step label, typically the tool name for tool steps. Becomes part of the trajectory token."
        },
        "content": {
          "type": "string",
          "default": "",
          "description": "Text produced at this step. Only read for agent-ish steps and for the terminal evaluation."
        },
        "source_pointer": {
          "type": "object",
          "description": "Free-form pointer back to this step in the source record.",
          "additionalProperties": true
        }
      }
    },
    "orphan_result": {
      "type": "object",
      "required": [
        "result_id"
      ],
      "additionalProperties": false,
      "properties": {
        "result_id": {
          "type": "string",
          "minLength": 1,
          "description": "The call id this stray result claims to answer."
        },
        "tool_name": {
          "type": "string",
          "description": "Tool the result appears to come from, if known. Documentation only: IA3 records orphans under '<unknown>'."
        },
        "content": {
          "description": "The stray result payload, if captured."
        },
        "source_pointer": {
          "type": "object",
          "description": "Pointer back to the stray result. Passed through as the finding's source_pointer.",
          "additionalProperties": true
        }
      }
    }
  }
}
